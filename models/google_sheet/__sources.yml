# models/google_sheet/__sources.yml
version: 2

sources:

  - name: google_sheet
    schema: google_sheet
    database: DB_27_BRONZE_DB 

    tables:

      - name: users
        config:
          loaded_at_field: fecha_alta_sistema
          freshness: 
              warn_after:
                count: 24
                period: hour
              error_after: 
                count: 48
                period: hour
        columns:
          - name: Nombre
          - name: DNI
          - name: email
          - name: fecha_alta_sistema
